# Plan 6 — RQ3 real-data faithfulness probe

**Goal:** Probe the real-data gate agent's decision mechanism (causal ablation vs.
SHAP/saliency method agreement) with visible activity diagnostics, on the full real
ETF panel. Design: `docs/design_2026-07-20_rq3-real-faithfulness.md`.

**Approach:** Small extension of the existing RQ3 harness — no new probe math. Reuse
`feature_groups`, `make_gate_fn`, `make_gate_mean_fn`, `rollout_observations`,
`run_probe`. Add two pure helpers + one experiment runner. Implement directly with TDD
in this session (scope too small for a full subagent cycle).

## Global constraints
- `uv` only. LSF CPU, thread-capped, never the login node.
- Safe sleeve resolved by ticker (IEF→TLT), never a hardcoded index (columns are alphabetical).
- No silent failures: the degenerate (constant-agent) case is logged and recorded, not swallowed.

## Task 1 — `_activity_diagnostics` (pure helper) + test
- **Files:** `src/rq3_faithfulness.py`, `tests/test_rq3_faithfulness.py`
- Signature: `_activity_diagnostics(gate_fn, observations, groups, seed=0) -> dict`
  returning `{"gate_mean", "gate_std", "causal_magnitude": {group: raw |freeze effect|}}`.
- Uses `gate_fn` for gate_mean/std, `causal_effect(gate_fn, obs, indices, "freeze", seed)`
  per group for the raw (un-normalized) magnitudes.
- **Test (known-answer):**
  - constant gate_fn ⇒ `gate_std == 0` and every `causal_magnitude` ~ 0.
  - signal-only gate_fn ⇒ `causal_magnitude["signal"]` > `causal_magnitude["returns"]`
    and > `["short_vol"]`.

## Task 2 — `_probe_or_null` degenerate guard (pure helper) + test
- **Files:** `src/rq3_faithfulness.py`, `tests/test_rq3_faithfulness.py`
- Signature: `_probe_or_null(gate_fn, gate_mean_fn, observations, groups, seed=0) -> dict`.
  Calls `run_probe`; on `ValueError` (importances sum to zero) prints the condition and
  returns `{"degenerate": True, "reason": <str>}`; else returns the normal verdict
  augmented with `{"degenerate": False}`.
- **Test:** a constant gate_mean_fn/gate_fn (zero gradient ⇒ zero saliency) ⇒ returned
  dict has `degenerate == True`; a signal-only stub ⇒ `degenerate == False` and the
  verdict shape is intact.

## Task 3 — `run_real_experiment` + `REAL_CONFIG`
- **Files:** `src/rq3_faithfulness.py`
- `REAL_CONFIG = {"base_name": "risk_parity", "window": 20, "cost_bps": 10.0,
  "total_timesteps": 150_000}` (safe_asset_index resolved at runtime; n_assets from data).
- `run_real_experiment(config, n_seeds)`:
  1. `load_etf_panel()`; resolve `safe_asset_index` by ticker (IEF→TLT, fail loud).
  2. `build_real_market(returns, safe_asset_index, window)` — signal on the full series.
  3. Per seed: `train_agent(market, {**config, "seed": seed, "safe_asset_index": ...})`;
     `rollout_observations`; build numpy + torch gate adapters;
     `verdict = _probe_or_null(...)`; `diag = _activity_diagnostics(...)`; attach diag to verdict.
  4. Aggregate via `_summarize_across_seeds` over non-degenerate seeds, plus a
     `diagnostics` block (mean/std gate, mean raw causal magnitude per group, degenerate count).
  5. Traceable run header; save `config.json` + `results.json` under a timestamped dir.
- Not unit-tested (trains PPO), consistent with `run_experiment`/`run_tilt_experiment`.
- **Docstring fix (surgical):** clarify `feature_groups` docstring that "signal = ground-truth
  driver" is synthetic-only; on real data it is a causal heuristic with no known true importance.

## Task 4 — LSF driver
- **Files:** `scripts/rq3_faithfulness_real.sh`
- Mirror `scripts/rq3_faithfulness_tilt.sh`: `set -euo pipefail`, thread-cap (OMP/MKL),
  `export PATH="$HOME/.local/bin:$PATH"`, run
  `uv run python -u -c "from src.rq3_faithfulness import run_real_experiment, REAL_CONFIG; run_real_experiment(REAL_CONFIG, 5)"`.

## Verification
- `uv run pytest -q` green (new tests + full suite).
- `bsub` the driver on `normal`; monitor to completion; read `results.json`.
- Document verdict (context/decisions.md, README, memory); commit; confirm push.
